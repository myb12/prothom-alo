import React from 'react';

const AdComponent = ({ addNumber }) => {
    return (
        <div className='add-component'>
            AD-{addNumber}
        </div>
    );
};

export default AdComponent;